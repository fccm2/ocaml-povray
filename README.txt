A basic OCaml interface for PovRay.

Before the first time you run povray you will maybe have to do:
 cp -rf /etc/povray ~/.povray

How to compile:
- in the directory src/ run the command:
   make

How to generate the documentation:
- in the directory src/ run the command:
   make doc

You can find some examples in the directory:
   examples/

